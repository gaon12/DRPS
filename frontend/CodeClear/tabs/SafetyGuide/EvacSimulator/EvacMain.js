import React from 'react';
import { View, Text } from 'react-native';

const EvacMain = () => {
	return (
		<View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
			<Text style={{ fontSize: 20 }}>끼에에엙</Text>
		</View>
	);
};

export default EvacMain;
